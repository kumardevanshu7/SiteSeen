# Local Firebase Admin credentials (gitignored)
#
# Download from Firebase Console → Project settings → Service accounts
# → Generate new private key, then save as:
#
#   scripts/serviceAccountKey.json
#
# On Vercel, do NOT upload this file. Instead add env vars from `.env.example`:
#   FIREBASE_SERVICE_ACCOUNT_JSON  (or the FIREBASE_ADMIN_* trio)
